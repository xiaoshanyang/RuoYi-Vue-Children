<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ActivityMonitorMapper">
    
    <resultMap type="com.ruoyi.system.domain.vo.SysActivityMonitorVo" id="ActivityMonitorResult">
        <result property="id"    column="id"    />
        <result property="activityId"    column="activity_id"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="remindTime"    column="remind_time"    />
        <result property="remark"    column="remark"    />
        <result property="createTime"    column="create_time"    />

        <result property="activityName"    column="activity_name"    />
        <result property="activityType"    column="activity_type"    />
        <result property="className"    column="class_name"    />
        <result property="activityPlace"    column="activity_place"    />
        <result property="presetDuration"    column="preset_duration"    />
        <result property="actualDuration"    column="actual_duration"    />
        <result property="activityStatus"    column="activity_status"    />
    </resultMap>

    <select id="selectMonitorList" resultMap="ActivityMonitorResult">
        SELECT m.*, a.activity_name, a.class_full_name, a.grade_name
        FROM activity_monitor m
                 LEFT JOIN activity a ON m.activity_id = a.activity_id
        ORDER BY m.monitor_id DESC
    </select>

    <insert id="insertActivityMonitor">
        INSERT INTO activity_monitor(
            activity_id, monitor_time, monitor_status,
            order_status, safe_hidden, child_emotion,
            abnormal_desc, handle_method, handle_result, monitor_by
        ) VALUES (
                     #{activityId}, #{monitorTime}, #{monitorStatus},
                     #{orderStatus}, #{safeHidden}, #{childEmotion},
                     #{abnormalDesc}, #{handleMethod}, #{handleResult}, #{monitorBy}
                 )
    </insert>
</mapper>